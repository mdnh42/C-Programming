//Write a C program to check whether a given number is positive or negative.

#include<stdio.h>
int main()
{
    int number;
    printf("Enter you a number: ");
    scanf("%d", &number);

    if(number>0)
    {
        printf("This is positive number");4. Write a C program to find whether a given year is a leap year or not.
    }
    else
    {
        printf("This is negetive numbre.");
    }
}
